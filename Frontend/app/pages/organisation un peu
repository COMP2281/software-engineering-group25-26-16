organisation un peu 

1. push cque jai enregistre add et commit (aka les chatbot dashboard upload et notfound)
relis d ailleurs le commit et demande is il est accurate 

2. question sur sidebar.txt ; global.css

3. ajouter de commentaires sur pages.css ; dashboard.css ; layout.tsx ; sidebar.css

4. wtf are routes.ts et root.tsx et jles commmit ou pas 

5. app.css utile ou pas ? jme suis kherbe9

6. verifie si ca ft ai tt ca psk sinon ca va pas etre tres drole quand on va se faire flag 

7. checklist: 
Create/confirm folder structure: pages/, components/,  styles/, services/, types/
CHECK NAN? 

Set up the router (main routes + nested routes if needed)
CA VEUT DIRE QUOI CA

Implement global layout in App.tsx
0 IDEE JAI PAS DE APP.TSX MAIS JAI GLOBAL.CSS DONC CEST LA MEME NAN? 

Sidebar: structure, items, active link styling, icons (if used)
OK JAI COMMENCÉ MAIS FAUT AVANCER QUOI 

Responsive sidebar (collapse / mobile overlay)
CEST PAS LA DERNIERE PHRASE DE SIDEBAR.CSS ETC ? CA: "@media (max-width: 768px) {
  .sidebar {
    width: var(--sidebar-width);
  }
}"

Add 404 route/page
CHECK

Add PageWrapper component (padding, title, breadcrumbs if needed)
JAI COMMENC2 YAK??

Add route guards (ProtectedRoute) if auth
JSMM PAS CQUE CA VEUT DIRE FORCE A MOI DANS MES PROJETS 

Verify navigation end-to-end (no dead links)

Short documentation: how to add a new page/route

Define CSS tokens in styles/global.css (colors, fonts, spacing)
OUI NORMALEMENT 

